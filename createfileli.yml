---
 - hosts: all
   tasks:
   - name: Clone a github repository
     git:
       repo: {{ repourl }}
       dest: {{ clondest }}
       clone: yes
       update: yes
   - name: Run shell script
     shell: "sh {{ dir }}/{{ shfile }} "
