---
 - hosts: all
   tasks:
   - name: Clone a github repository
     git:
       repo: https://github.com/yogitachourey123/linuxdemo2.git
       dest: /home/hcligt/direc
       clone: yes
       update: yes
   - name: Run shell script
     shell: "sh {{ dir }}/{{ shfile }} {{ dir }}"
